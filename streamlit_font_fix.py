import streamlit as st

# ตั้งค่าหน้าเว็บ (ควรอยู่บรรทัดแรกๆ ของโค้ด Streamlit)
st.set_page_config(
    page_title="Font Fallback Test",
    page_icon="🎨",
    layout="centered"
)

# ---------------------------------------------------------
# ส่วนการจัดการ CSS เพื่อทำ Font Fallback
# ---------------------------------------------------------
def inject_custom_css():
    """
    ฟังก์ชันสำหรับฝัง CSS เพื่อบังคับใช้ฟอนต์และกำหนด Fallback
    สำหรับสัญลักษณ์ต่างๆ ให้แสดงผลได้ถูกต้องบนทุก OS
    """
    custom_css = """
    <style>
        /* นำเข้าฟอนต์ภาษาไทยจาก Google Fonts (ตัวอย่างใช้ 'Sarabun') */
        @import url('https://fonts.googleapis.com/css2?family=Sarabun:wght@400;700&display=swap');

        /* กำหนด font-family ให้กับองค์ประกอบหลักของ Streamlit */
        html, body, [class*="css"] {
            font-family: 'Sarabun',          /* 1. ฟอนต์หลักภาษาไทย */
                         'Segoe UI Emoji',   /* 2. Fallback สำหรับ Windows Emojis/Symbols */
                         'Apple Color Emoji',/* 3. Fallback สำหรับ macOS Emojis */
                         'Noto Color Emoji', /* 4. Fallback สำหรับ Linux/Android */
                         sans-serif;         /* 5. ฟอนต์พื้นฐานหากไม่เจออะไรเลย */
        }
        
        /* เพิ่มเติม: ปรับขนาดฟอนต์หัวข้อให้ชัดเจนขึ้น */
        h1, h2, h3 {
            font-family: 'Sarabun', 'Segoe UI Emoji', sans-serif !important;
        }
    </style>
    """
    st.markdown(custom_css, unsafe_allow_html=True)

# เรียกใช้ฟังก์ชัน CSS
inject_custom_css()

# ---------------------------------------------------------
# ส่วนเนื้อหาของแอพพลิเคชัน (Main App Content)
# ---------------------------------------------------------

st.title("ทดสอบการแสดงผลฟอนต์และสัญลักษณ์ 🎨")

st.markdown("### 1. ทดสอบภาษาไทย")
st.write("สวัสดีครับ นี่คือข้อความทดสอบฟอนต์ภาษาไทย 'Sarabun' ครับ อ่านง่ายและสบายตา")

st.markdown("### 2. ทดสอบสัญลักษณ์และ Emojis (Fallback Fonts)")
st.info("หาก CSS ทำงานถูกต้อง สัญลักษณ์ด้านล่างควรแสดงเป็นรูปภาพสี (Emoji) หรือสัญลักษณ์ที่ถูกต้อง ไม่ใช่สี่เหลี่ยม")

# ตัวอย่างสัญลักษณ์หมวดต่างๆ
col1, col2 = st.columns(2)

with col1:
    st.success("✅ เครื่องหมายถูก (Check)")
    st.warning("⚠ คำเตือน (Warning)")
    st.error("❌ ยกเลิก (Cross)")

with col2:
    st.write("🚀 Rocket")
    st.write("📊 Chart")
    st.write("💻 Laptop")

st.markdown("---")
st.markdown("### 3. ทดสอบสัญลักษณ์ทางคณิตศาสตร์")
st.write("สัญลักษณ์เหล่านี้มักจะใช้ฟอนต์ระบบพื้นฐานหากฟอนต์ไทยไม่มี:")
st.code("∑ (Sum)  |  ∏ (Product)  |  ∞ (Infinity)  |  ≠ (Not Equal)  |  ≤ (Less than or equal)")

st.write("หวังว่าวิธีนี้จะช่วยแก้ปัญหาการแสดงผลสัญลักษณ์ของคุณแจนได้นะครับ! 😊")
