import streamlit as st
import pandas as pd
import re
from datetime import datetime

# --- Helper Functions ---
def clean_string(val):
    """ลบช่องว่างหัวท้าย แปลงเป็น string"""
    if pd.isna(val): return ""
    return str(val).strip()

def normalize_db_name_field(full_name_str):
    """
    แปลงค่าใน DB 'ชื่อ-สกุล' ให้เป็น (ชื่อ, นามสกุล)
    โดยจัดการกับช่องว่างที่อาจจะเกินมาตรงกลาง
    เช่น 'สมชาย   รักดี ' -> ('สมชาย', 'รักดี')
    """
    clean_val = clean_string(full_name_str)
    # ใช้ split() โดยไม่ระบุ parameter จะตัด whitespace ทุกรูปแบบ (space, tab, multiple spaces)
    parts = clean_val.split()
    
    if len(parts) >= 2:
        # กรณีปกติ หรือมีชื่อกลาง (เอาตัวแรกเป็นชื่อ ตัวสุดท้ายเป็นนามสกุล)
        # หรือถ้าต้องการละเอียดกว่านี้ อาจจะต้องตกลง format ใน db อีกที
        first_name = parts[0]
        last_name = " ".join(parts[1:]) # เผื่อกรณีมีชื่อกลางหรือนามสกุลยาว
        return first_name, last_name
    elif len(parts) == 1:
        return parts[0], ""
    else:
        return "", ""

def check_registration_logic(df, input_fname, input_lname, input_id):
    """
    ตรวจสอบข้อมูล 3 ช่อง กับ Database
    Return: (Success: bool, Message: str, UserData: dict/None)
    """
    # 1. เตรียมข้อมูล Input
    i_fname = clean_string(input_fname)
    i_lname = clean_string(input_lname)
    i_id = clean_string(input_id)

    if not i_fname or not i_lname or not i_id:
        return False, "กรุณากรอกข้อมูลให้ครบทั้ง 3 ช่อง", None

    if len(i_id) != 13:
        return False, "เลขบัตรประชาชนต้องมี 13 หลัก", None

    # 2. กรองด้วยเลขบัตรประชาชนก่อน (แม่นยำที่สุด)
    # ใช้ str.strip() เพื่อความชัวร์เรื่องช่องว่างใน DB
    user_match = df[df['เลขบัตรประชาชน'].astype(str).str.strip() == i_id]

    if user_match.empty:
        return False, "ไม่พบข้อมูลเลขบัตรประชาชนนี้ในระบบ", None

    # 3. ตรวจสอบชื่อและนามสกุล (จากคนที่เลขบัตรตรงกัน)
    # วนลูปเช็ค (เผื่อกรณีเลขบัตรซ้ำ ซึ่งไม่ควรมี แต่กันไว้ดีกว่า)
    for index, row in user_match.iterrows():
        db_fname, db_lname = normalize_db_name_field(row['ชื่อ-สกุล'])
        
        # เปรียบเทียบแบบ String Exact Match
        if db_fname == i_fname and db_lname.replace(" ", "") == i_lname.replace(" ", ""):
            # เจอแล้ว! ส่งข้อมูลกลับ
            return True, "ลงทะเบียนสำเร็จ", row.to_dict()
    
    # ถ้าหลุดลูปมาแสดงว่าเลขบัตรถูก แต่ชื่อหรือนามสกุลไม่ตรง
    return False, "ข้อมูลชื่อหรือนามสกุล ไม่ตรงกับฐานข้อมูล (กรุณาตรวจสอบตัวสะกด)", None

def render_registration_page(df):
    """แสดงหน้า UI สำหรับการลงทะเบียนผ่าน LINE"""
    st.markdown("""
    <style>
        .reg-container {
            background-color: white;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            max-width: 500px;
            margin: auto;
        }
        .reg-header {
            color: #00B900; /* LINE Green */
            text-align: center;
            font-weight: bold;
            margin-bottom: 1.5rem;
        }
        .stButton>button {
            background-color: #00B900 !important;
            color: white !important;
            border-radius: 50px;
            height: 50px;
            font-size: 18px;
        }
    </style>
    """, unsafe_allow_html=True)

    # เช็คว่าลงทะเบียนสำเร็จหรือยัง
    if st.session_state.get('line_register_success', False):
        st.success("✅ ลงทะเบียนเรียบร้อยแล้ว!")
        user_info = st.session_state.get('line_registered_user', {})
        st.write(f"สวัสดีคุณ {user_info.get('ชื่อ-สกุล', '')}")
        st.info("ระบบได้ทำการเชื่อมต่อบัญชี LINE ของท่านเรียบร้อยแล้ว ท่านสามารถปิดหน้านี้ได้ทันที")
        # ในการใช้งานจริง ตรงนี้จะยิง API กลับไปหา LINE Messaging API เพื่อผูก UserID
        return

    # Container หลัก
    with st.container():
        st.markdown("<h2 class='reg-header'>ลงทะเบียนเชื่อมต่อบัญชี</h2>", unsafe_allow_html=True)
        
        # --- ส่วน PDPA ---
        with st.expander("📄 ข้อตกลงและเงื่อนไข (PDPA)", expanded=False):
            st.markdown("""
            **การเก็บรวบรวมข้อมูลส่วนบุคคล**
            1. ข้าพเจ้ายินยอมให้รพ.สันทราย เก็บข้อมูล ชื่อ-นามสกุล และเลขบัตรประชาชน เพื่อใช้ในการยืนยันตัวตน
            2. ข้อมูลสุขภาพจะถูกแสดงเฉพาะเจ้าของข้อมูลที่ยืนยันตัวตนถูกต้องเท่านั้น
            3. ระบบมีการรักษาความปลอดภัยตามมาตรฐาน
            """)
        
        pdpa_check = st.checkbox("ข้าพเจ้ายอมรับข้อตกลงและเงื่อนไข (PDPA)")

        st.markdown("---")

        # --- ส่วนกรอกข้อมูล 3 ช่อง ---
        with st.form("line_reg_form"):
            st.write("กรุณากรอกข้อมูลให้ตรงกับบัตรประชาชน")
            
            c1, c2 = st.columns(2)
            with c1:
                input_fname = st.text_input("ชื่อ (ไม่ต้องระบุคำนำหน้า)", placeholder="สมชาย")
            with c2:
                input_lname = st.text_input("นามสกุล", placeholder="ใจดี")
            
            input_id = st.text_input("เลขบัตรประชาชน (13 หลัก)", max_chars=13, placeholder="xxxxxxxxxxxxx")
            
            submit_btn = st.form_submit_button("ยืนยันตัวตน", use_container_width=True)

        if submit_btn:
            if not pdpa_check:
                st.warning("⚠️ กรุณากดยอมรับเงื่อนไข PDPA ก่อนลงทะเบียน")
            else:
                success, msg, user_row = check_registration_logic(df, input_fname, input_lname, input_id)
                
                if success:
                    # บันทึกสถานะ
                    st.session_state['line_register_success'] = True
                    st.session_state['line_registered_user'] = user_row
                    
                    # ถ้าต้องการ Login เข้าสู่ระบบหลักเลย ให้ set ค่าเหล่านี้
                    st.session_state['authenticated'] = True
                    st.session_state['pdpa_accepted'] = True
                    st.session_state['user_hn'] = user_row['HN']
                    st.session_state['user_name'] = user_row['ชื่อ-สกุล']
                    
                    st.rerun()
                else:
                    st.error(f"❌ {msg}")
